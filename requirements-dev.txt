# Development dependencies.
# Runtime deps are NOT listed here: they live in pyproject.toml and are pulled in
# by compile-deps.sh, which passes pyproject.toml alongside this file to pip-compile.

# Build tools
setuptools-scm>=10.0.5
pip-tools>=7.6.1

# Static analysis and code quality
mypy>=1.20.2
types-requests>=2.33.0.20260408
types-PyYAML>=6.0.12
ruff>=0.1.0
black>=26.3.1

# Testing
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
